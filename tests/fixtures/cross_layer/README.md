This directory should contain `sample.apk` — a minimal Android app
exposing one Kotlin `external fun` bound statically. See
tests/integration/test_cross_layer_apk.py for the contract. Build
manually with the NDK sample template; keep the file under 200 KB.
