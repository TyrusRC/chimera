<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vulnerable"
    android:versionCode="1" android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:usesCleartextTraffic="true">

        <activity android:name=".ExportedActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="vulnerable" android:host="callback"/>
            </intent-filter>
        </activity>

        <activity android:name=".SafeActivity"
            android:exported="false"/>

        <receiver android:name=".ExportedReceiver"
            android:exported="true"/>

        <provider android:name=".DataProvider"
            android:exported="true"
            android:authorities="com.example.vulnerable.provider"/>

        <service android:name=".BackgroundService"
            android:exported="true"
            android:permission="com.example.vulnerable.ADMIN"/>

    </application>
</manifest>
